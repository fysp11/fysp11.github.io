---
import AIGenerator from "./AIGenerator.tsx"

export const prerender = false

const initialPrompt = Astro.url.searchParams.get("prompt") || ""
---

<AIGenerator client:visible initialPrompt={decodeURIComponent(initialPrompt)} />
