---
import { cn } from "@/lib/utils"

interface Props {
  className?: string
  id?: string
}

const { className, id } = Astro.props
---

<main id={id} class={cn("mx-auto px-4", className)}>
  <slot />
</main>
